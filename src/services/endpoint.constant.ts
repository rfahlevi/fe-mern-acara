const endpoint = {
  AUTH: "/auth",
  BANNERS: "/banners",
  CATEGORIES: "/categories",
  EVENTS: "/events",
  MEDIA: "/media",
  REGIONS: "/regions",
  TICKETS: "/tickets",
};

export default endpoint;
